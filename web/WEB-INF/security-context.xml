<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <bean id="bcryptPasswordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>
    <security:http>

        <security:intercept-url pattern="/user/login" access="permitAll"/>

        <security:intercept-url pattern="/user/modifyForm" access="hasAnyRole('ROLE_MEMBER','ROLE_ADMIN')"/>

        <!--<security:form-login />-->
        <security:form-login login-page="/user/loginForm"/>

        <security:logout logout-url="/user/logout" invalidate-session="true"/>

        <security:access-denied-handler error-page="/accessError"/>
    </security:http>

    <security:authentication-manager>

        <security:authentication-provider>

            <security:jdbc-user-service data-source-ref="dataSource"
                                        users-by-username-query="select user_no, user_id, user_pw, enabled from tbl_user where user_no = ?"
                                        authorities-by-username-query="select user_no, auth from tbl_user_auth where user_no = ?"/>
            <security:password-encoder ref="bcryptPasswordEncoder"/>

        </security:authentication-provider>

    </security:authentication-manager>


</beans>